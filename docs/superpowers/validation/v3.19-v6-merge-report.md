# v3.19 V6 Merge — Operator Validation Report (Phase 7 Lite)

**Date:** 2026-05-28
**Branch:** dev (post-merge, HEAD = 8bcc98a)
**Validator:** Reno (via Claude Code subagent)

---

## Scope

Phase 7 LITE — verify V6 backend wire-up dispatches correctly, without full audio transcription metric capture. Full metric validation (賽馬 entity accuracy + Winning Factor segment metrics) deferred to manual operator run.

## Environment

- Qwen3 subprocess venv: `backend/scripts/v5_prototype/venv_qwen/bin/python` — **EXISTS**
- `V6_AVAILABLE`: **True** (no WARNING printed at boot)
- Backend boots cleanly (no import errors, no V6 missing-dependency crash)
- 20 V6 pipelines registered (pipeline_type=`v6_vad_dual_asr`), including 2 canonical:
  - `641a77ec` — `[v6] Winning Factor (English)`
  - `4696bbaa` — `[v6] 賽馬廣播 (Cantonese)`

---

## Smoke Test Results

### Backend boot
- ✓ Backend boots cleanly with V6 managers initialized (PipelineManager, TranscribeProfileManager, LLMProfileManager, RefinerProfileManager)
- ✓ Qwen3 venv present — no `[V6] WARNING` at boot
- ✓ `V6_AVAILABLE=True` set in app.config

### API endpoints
- ✓ `GET /api/health` → 200, status=ok
- ✓ `GET /api/me` returns `active_kind="profile"`, `active_id="dev-default"`, `v6_available=True`
- ✓ `GET /api/pipelines` lists 20 V6 pipelines (`pipeline_type=v6_vad_dual_asr`), total 24 pipelines
- ✓ `POST /api/active {"kind":"pipeline_v6","id":"641a77ec-..."}` → 200, `{"ok":true,"active":{"id":"641a77ec-...","kind":"pipeline_v6"}}`
- ✓ After activation, `/api/me` returns `active_kind="pipeline_v6"`, `active_id="641a77ec-..."`
- ✓ `config/settings.json` on disk writes `"active_kind":"pipeline_v6"` and `"active_id":"641a77ec-..."` correctly
- ✓ Restore `POST /api/active {"kind":"profile","id":"dev-default"}` → 200, settings.json reverts correctly

### Dispatch logic (mock-verified)
- ✓ `_asr_handler(job)` with `file.active_kind="pipeline_v6"` instantiates `PipelineRunner` and calls `._run_v6(user_id=1, cancel_event=...)` — verified via `unittest.mock.patch('pipeline_runner.PipelineRunner')`
- ✓ `_asr_handler(job)` with `file.active_kind="profile"` routes to `transcribe_with_segments` (unchanged path) — verified via `unittest.mock.patch('app.transcribe_with_segments')`
- ✓ No regression on profile-mode dispatch

---

## Verdict

**✓ PASS** — V6 backend dispatch is correctly wired end-to-end.

All four smoke test categories passed:
1. Backend boots cleanly with V6 components
2. `/api/me` surfaces `active_kind` + `v6_available` correctly
3. `/api/active` writes `pipeline_v6` to settings.json and is reflected in `/api/me`
4. `_asr_handler` correctly branches on `active_kind="pipeline_v6"` to `PipelineRunner._run_v6` (not `transcribe_with_segments`)

---

## Deferred (for manual operator validation)

The following metrics from the feat branch v6-validation were NOT re-captured in this lite run:

| Metric | Expected (feat branch baseline) | Status |
|--------|----------------------------------|--------|
| 賽馬 4-min Cantonese: VAD region count | ~8–12 regions | PENDING |
| 賽馬 Stage 1A: entity accuracy (袁幸堯/史滕雷/HIGHLAND BLINK) | >90% | PENDING |
| 賽馬 Stage 2: final segment count | ~40–60 segments | PENDING |
| 賽馬 Stage 3: refiner drop rate | <10% | PENDING |
| Winning Factor 14-min English: segment count | ~180–220 segments | PENDING |
| Winning Factor: avg segment duration | 3–6s | PENDING |

**To run manually:**
1. Start backend: `cd backend && source venv/bin/activate && export $(cat .env | xargs) && python app.py`
2. Dashboard → Pipeline preset → activate `[v6] 賽馬廣播 (Cantonese)`
3. Upload the 賽馬 audio file → wait for pipeline completion → inspect proofread page
4. Repeat for `[v6] Winning Factor (English)` pipeline + corresponding audio
5. Compare segment counts, entity names, and timing against feat branch baseline
