openai-whisper>=20231117
faster-whisper>=1.0.0
flask>=3.0.0
flask-cors>=4.0.0
flask-socketio>=5.3.6
werkzeug>=3.0.0
eventlet>=0.35.0
numpy>=1.24.0
torch>=2.0.0
torchaudio>=2.0.0
ffmpeg-python>=0.2.0
python-socketio>=5.10.0
gevent>=23.9.0
gevent-websocket>=0.10.1
pysbd>=0.3.4
opencc-python-reimplemented>=0.1.7
# Optional: streaming mode (low-latency live transcription)
whisper-streaming>=0.1.0
librosa>=0.10.0
soundfile>=0.12.0
# R5 Phase 1: server mode auth + password hashing
Flask-Login==0.6.3
bcrypt==4.1.2
# Phase 6: rate limiting
Flask-Limiter>=3.5.0

# V6 — VAD + Qwen3-ASR subprocess audio I/O (v3.19)
silero-vad>=6.2.1

# Subtitle custom-font upload: read the real family name from a font's `name`
# table so the live preview (@font-face) and burn-in (libass :fontsdir) match.
fonttools>=4.40.0

# Token licensing — Ed25519 offline signature
PyNaCl>=1.5.0
